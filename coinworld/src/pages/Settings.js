import React from 'react';


class Settings extends React.Component {
  render() {
    return (
      <section id="settings">
        <h2>Settings Page</h2>
        <p>On the todo list, will include saving display options.</p>
      </section>

    );
  }
}

export default Settings;